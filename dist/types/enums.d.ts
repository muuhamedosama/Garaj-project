export declare enum UserType {
    admin = 0,
    Customer = 1,
    ServiceProvider = 2
}
export declare enum BookingStatus {
    Pending = "pending",
    Completed = "completed",
    Canceled = "canceled"
}
